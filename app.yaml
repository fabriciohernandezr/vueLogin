runtime: nodejs12
handlers:
# Serve all static files with url ending with a file extension
- url: /(.*\.(js|css|png|jpg|svg|ttf|ico))$
  static_files: \1
  upload: .*\.(js|css|png|jpg|svg|ttf|ico)$
# Catch all handler to index.html
- url: /.*
  static_files: index.html
  upload: index.html